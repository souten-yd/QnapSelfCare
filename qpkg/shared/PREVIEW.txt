QnapSelfCare 0.3.18

Local health records, OMRON history transfer, personalized trend lines, wellness goals, food notes and AI coaching.
UI: http://<NAS-IP>:17863/ (no login). Data: /share/Container/QnapSelfCare.
Use QnapHomeHub 0.3.5 shared USB radio service for diagnostic sync on the same dongle.
HomeHub UI stays on 8787; the shared radio uses a Unix socket, no public port.
ESP32 is not used. Optional standalone USB setup is in bluetooth/ and scripts/.
Hardware pairing and measurement synchronization require validation on the NAS.
Read README.md and docs/bluetooth-homehub.md before pairing.

Settings > SelfCare update: check, then apply from the Web UI.
The NAS downloads and verifies the QPKG, backs up data, installs and verifies restart.
Upgrading from 0.3.0 requires one initial manual App Center install.
Update logs/backups: /share/Container/QnapSelfCare/updates/. NAS self-update needs real-device validation.

Verified periodic SQLite/config backups: every 7 days, 14 generations by default.
Settings include integrity/disk/backup diagnostics, manual backup and archive verification.
Corruption blocks writes and new collection while preserving recovery Web access.
Offline recovery: selfcare-backup verify/restore; see docs/data-protection.md.
Keep downloaded backups outside the NAS; shared radio bonding data needs a separate HomeHub backup.

Settings > Shared Bluetooth setup migrates standard HomeHub Compose deployments.
Preserves settings, checks health and offers recovery; see docs/homehub-migration.md.
Backup waits and retries while an update or migration holds the lock.
