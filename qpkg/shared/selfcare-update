#!/bin/sh
set -eu
if ! command -v python3 >/dev/null 2>&1; then
    echo 'Python 3 is required to run the QnapSelfCare update checker.' >&2
    exit 1
fi
exec python3 "$(dirname "$0")/updater.py" "$@"
