name: check
on:
  pull_request:
    paths:
      - '*.py'
      - 'bluetooth/**'
      - 'homehub/**'
      - 'licenses/**'
      - 'requirements-ble.txt'
      - 'package*.json'
      - 'tests/**'
      - 'qpkg/**'
      - 'scripts/**'
      - 'web/**'
      - 'webapp.py'
      - '.github/workflows/check.yml'
  push:
    branches: [main]
    paths:
      - '*.py'
      - 'bluetooth/**'
      - 'homehub/**'
      - 'licenses/**'
      - 'requirements-ble.txt'
      - 'package*.json'
      - 'tests/**'
      - 'qpkg/**'
      - 'scripts/**'
      - 'web/**'
      - 'webapp.py'
      - '.github/workflows/check.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m unittest discover -s tests -v
      - run: python -m py_compile storage.py collectors.py ble_protocol.py ble_worker.py ble_bridge.py updater.py self_update.py durability.py protection.py homehub_migration.py
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm ci --no-audit --no-fund
      - run: npm test
      - run: node --check web/app.js && node --check web/update.js && node --check web/protection.js && node --check web/migration.js
      - run: python -m py_compile webapp.py
      - run: docker compose --project-directory homehub -f homehub/compose-shared.yaml config --quiet
      - run: docker compose --project-directory homehub -f homehub/compose-legacy.yaml config --quiet
      - run: sh -n qpkg/shared/selfcare.sh scripts/install-ble.sh bluetooth/entrypoint.sh
      - run: sh -n qpkg/shared/selfcare-backup qpkg/shared/selfcare-update qpkg/shared/python3-path
      - name: Install pinned QDK
        run: |
          curl --fail --location --retry 3 --output /tmp/qdk.deb https://github.com/qnap-dev/QDK/releases/download/v2.5.3/qdk_2.5.3_amd64.deb
          echo '17b3841b7d4590a4ee025844ba583304b5e3c497d9fa8934d5175131d3908022  /tmp/qdk.deb' | sha256sum -c -
          sudo apt-get update -qq
          sudo apt-get install -y /tmp/qdk.deb rsync
      - run: bash scripts/build-qpkg.sh x86_64 "$PWD/dist"
      - run: bash scripts/build-qpkg.sh arm_64 "$PWD/dist"
      - name: Publish release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view v0.3.4 >/dev/null 2>&1; then
            echo 'Release v0.3.4 already exists; leaving it unchanged.'
            exit 0
          fi
          sha256sum dist/*.qpkg > dist/SHA256SUMS
          gh release create v0.3.4 dist/*.qpkg dist/SHA256SUMS \
            --title 'QnapSelfCare 0.3.4' \
            --notes 'Allows the HomeHub shared-Bluetooth migration to preserve supported customized Compose settings. Validates Bluetooth ownership, retains the original Compose for rollback, and detects configuration changes during image retrieval. NAS migration still requires hardware validation.' \
            --target "$GITHUB_SHA"
